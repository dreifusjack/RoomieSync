export interface User {
  id: string;
  email: string;
  groupId: string
  firstName: string;
  lastName: string;
}

export interface UserDetails {
  user_id: string;
  first_name: string;
  last_name: string;
  email: string;
};
